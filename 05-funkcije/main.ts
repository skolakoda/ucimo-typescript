function saberi(x: number, y: number): number {
    return x + y
}

function pozdrav(ime: any): string {
    return 'Zdravo korisniče ' + ime
}

console.log( saberi(4, 5) )

console.log( pozdrav("Goran") )